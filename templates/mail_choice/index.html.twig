{% extends 'layout/layout-center.html.twig' %}

{% block title %}mailChoice!
{% endblock %}


{% block content %}
	<div class="white-stripe fixed-top text-center mt-4">
		<img src="{{ asset('build/images/logo.png') }}" alt="">
	</div>
	<div class="container">
<div class="row row-cols-lg-3 row-cols-md-2 row-cols-1">
            <div class="col m-auto text-center">
            <div class="col m-auto fw-bold text-center h4 text-light mb-4">As-tu une adresse mail ?</div>
				<button type="button" class="btn btn-secondary mb-4 btn-lg w-100 text-uppercase">J'ai une adresse mail</button>
				<button type="button" class="btn btn-secondary mb-4 btn-lg w-100 text-uppercase">Je n'ai pas d'adresse mail</button>
			</div>
		</div>
	</div>


{% endblock %}
