{% if app.user %}
<ul class="navbar fixed-top justify-content-between list-unstyled">
    <li>
        <div class="ms-5">
            <img src="{{ asset('build/images/logo_v2.png') }}" alt="" class="mt-2">
        </div>
    </li>
    <li>
        <div class="btn-group me-5">
            <button type="button" class="btn btn-light dropdown-toggle" id="avatarDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="fa-solid fa-user"></i>
            </button>

            <ul class="dropdown-menu dropdown-menu-end">
                <li>
                    <a class="dropdown-item" href="#">Mon profil</a>
                </li>
                <li>
                    <a class="dropdown-item" href="#">Choisir mon avatar</a>
                </li>
                <li>
                    <a href="{{ path('app_logout')  }}" class="dropdown-item">Déconnexion</a>
                </li>
            </ul>
        </div>
    </li>
</ul>

{% block javascripts %}
    <script src="{{ asset('build/app.js') }}"></script>
    <script src="{{ asset('build/bootstrap.js') }}"></script>

    <script>
        var avatarDropdown = document.getElementById('avatarDropdown');
        var avatarImg = document.createElement('img');
        avatarImg.src = "{{ asset('build/avatars/' ~ app.user.avatar.getPath()) }}";
        avatarImg.alt = "Avatar de l'utilisateur";

        avatarDropdown.innerHTML = '';
        avatarDropdown.appendChild(avatarImg);
    </script>
{% endblock %}
{% endif %}

<ul class="navbar fixed-bottom navbar-light bg-blue d-flex justify-content-around">
    <li class="nav-item list-unstyled ">
        <a class="nav-link d-flex align-items-center flex-column-reverse text-light"
           href="{{ path('index_category') }}">
            Accueil
            <i class="fa-solid fa-home fa-3x"></i>
        </a>
    </li>
    <li class="nav-item list-unstyled">
        <a class="nav-link d-flex align-items-center flex-column-reverse text-light" href="#">
            Retour
            <i class="fa-solid fa-arrow-left-long fa-3x"></i>
        </a>
    </li>
</ul>
