{% extends 'layout/layout-center.html.twig' %}

{% block title %}Résultat du Quiz{% endblock %}

{% block body %}
	<div class="row">
		<h1 class="text-center m-auto mb-5 text-secondary">Tutoriel : <br> {{ tutorial.name }}</h1>
		<div class="bg-secondary p-2 p-md-4 w-75 text-center m-auto mt-2 rounded-4">
			{% if successPercentage>=80 %}
				<h2 class="text-center mb-md-3 text-uppercase">Score:
					{{ successPercentage }}
					% <i class="fa-solid fa-circle-check text-success"></i>
				</h2>
				<div class="bg-primary text-light w-100 m-auto p-md-5 p-3 rounded-4 mb-md-5">
					<h3 class="mb-3 mb-md-5 text-uppercase">Bravo !</h3>
					<p>
						Ton score est de
						{{ score }}/5.<br>
						Bravo ! <a class="text-white" href="{{ path('index_category') }}">Clique ici</a> pour retourner sur l'accueil
					</p>
				</div>
			{% else %}
				<h2 class="text-center mb-md-3 text-uppercase">Score:
					{{ successPercentage }}
					% <i class="fa-solid fa-circle-xmark text-danger"></i>
				</h2>
				<div class="bg-primary text-light w-100 m-auto p-md-5 p-3 rounded-4 mb-md-5">
					<h3 class="mb-3 mb-md-5 text-uppercase">Essaye encore..</h3>
					<p>
						Ton score est de
						{{ score }}/5.<br>💪 Ne soyez pas découragé par votre résultat au test,
															vous êtes sur la bonne voie ! Reprenez le défi et montrez à tous vos incroyables capacités. Vous êtes plus fort que vous ne le pensez, alors n'abandonnez pas, recommencez et laissez briller votre potentiel !
					</p>
					<a type="button" href="javascript:history.go(-1)" class="m-auto next-btn p-2 border-0 bg-success rounded-4 w-75 text-light text-decoration-none">
						Clique ici pour réessayer le quiz
					</a>
				</div>
			{% endif %}
		</div>
	</div>
{% endblock %}

